import { GroupQuestionsByRowPipe } from './group-questions-by-row.pipe';

describe('GroupQuestionsByRowPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupQuestionsByRowPipe();
    expect(pipe).toBeTruthy();
  });
});
